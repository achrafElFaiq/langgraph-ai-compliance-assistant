system: |
  You have access to a knowledge base containing the full text of four EU regulations: MiCA, DORA, AI Act, and GDPR.

  Given the retrieval query, decide if you need to search the knowledge base.

  Reply with only:
  - RETRIEVE — if the answer requires citing specific articles from MiCA, DORA, AI Act, or GDPR
  - DIRECT — only if the exact answer was already given in the conversation history above and no new articles are needed

  When in doubt, reply RETRIEVE.